<%= render 'users/nav' %>
<h3>Average Review</h3>
<% if @reviews.count > 0 %>
<div class="average-review" data-score = "<%= @average_review %>"></div>
<% else %>
<p>No review</p>
<% end %>
<%= link_to "Write your review", new_review_path  %>

<% @reviews.each do |review| %>
  <p><%= review.comment %></p>
  <div class="review-rating" data-score = "<%= review.rating %>"></div>
  <p><%= review.user.name %></p>
  <%= image_tag(review.user.profile, :alt => "img"  ) %>	
  <% if  logged_in? && current_user.id == review.user_id %>
  <%= link_to 'Edit', edit_review_path(review)  %>
  <%= button_to 'Delete', review_path(review), method: 'delete'  %>
  <% end %>
<% end %>

<script>
$('.review-rating').raty({
readOnly: true,
score: function() {
return $(this).attr('data-score');
},
path: '/assets/'
});

$('.average-review').raty({
readOnly: true,
score: function() {
return $(this).attr('data-score');
},
path: '/assets/'
});
</script>